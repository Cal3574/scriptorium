// @scriptorium/database owns all Postgres access: the connection pool, the
// schema, migrations, seed data and typed query helpers. Nothing else in the
// workspace opens a database connection directly.
//
// No product schema yet.
export {};
