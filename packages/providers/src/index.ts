// @scriptorium/providers holds adapters for external services the product
// depends on (LLM completions, embeddings, object storage). Adapters are
// stateless and never touch the database - persistence is owned by the
// layers above them, which is enforced by the module boundary rules.
//
// No adapters yet.
export {};
